---
- name: build docker image
  docker_image:
    name: Myapp
    source: build
    build:
      path: "{{ dockerpath }}"
      pull: no
    state: "present"
  tags: "http://192.168.122.41:8082/Myapp"
